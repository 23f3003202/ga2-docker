name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.chetnasuthar07 }}
          password: ${{ secrets.dckr_pat_uVRM0qURzX7o99KCMa-hNDueD4w }}

      - name: Build and Push
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: chetnasuthar07/ga2-docker:23f3003202
